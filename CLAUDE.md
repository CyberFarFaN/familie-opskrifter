# familie-opskrifter

## Memory System
Central memory lives in the claude-workspace repo.

## Key rules
- Graph-first, PowerShell 7 only, no auto-install
- Never hardcode tenant-specific values
- Never commit secrets, tokens, tenant IDs
- Terse responses, challenge bad approaches, max 1 clarifying question
