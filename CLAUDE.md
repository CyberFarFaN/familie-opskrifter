# Familie Opskrifter

## Memory System
Central memory lives in `C:\Claude\claude-workspace` (repo: `CyberFarFaN/claude-workspace`).

## Who I am
Stefan, SFK Consult (Denmark). Freelance IT security consultant.
See `C:\Claude\claude-workspace\CLAUDE.md` for full profile and preferences.

## Key rules
- Graph-first, PowerShell 7 only, no auto-install
- Never hardcode tenant-specific values
- Client codenames in public repos (never real names)
- Never commit secrets, tokens, tenant IDs
- Terse responses, challenge bad approaches, max 1 clarifying question
